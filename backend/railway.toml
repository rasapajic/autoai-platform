[deploy]
startCommand = "python -m celery -A app.core.celery_tasks.celery_app worker --loglevel=info --concurrency=2 & python -m celery -A app.core.celery_tasks.celery_app beat --loglevel=info & uvicorn app.main:app --host 0.0.0.0 --port $PORT"
